import styled from "styled-components"

export const Container = styled.button`
  width: 24px;
  height: 24px;

  background: transparent;
  border: none;
  > svg {
    width: 100%;
    height: auto;
  }
`
